#!/usr/bin/env bash
# LaunchAgent leftover wrapper. Prefer ./setup.sh (binary reads ~/.config/conduit/.env).
set -euo pipefail
exec "${HOME}/.local/bin/conduit" -config "${HOME}/.config/conduit/config.toml"
